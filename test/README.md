# Test data layout

This directory contains:

- `toy_tiny/`: a tiny Bookshelf example with intentional overlaps. It is small enough for students to debug by hand.
- `benchmarks/AES`, `benchmarks/JPEG`, `benchmarks/GCD`: bundled course benchmark cases.

For benchmark case details and expected file layout, see `test/benchmarks/README.md`.
