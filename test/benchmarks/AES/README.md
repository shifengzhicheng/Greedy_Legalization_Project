# AES benchmark

本目录包含 AES / NG45 的 Bookshelf benchmark。

应包含：

- `AES.aux`
- `AES.nodes`
- `AES.nets`
- `AES.pl`
- `AES.scl`

运行示例：

```bash
bash run.sh AES
python main.py --case AES --mode baseline --out-dir results
```
