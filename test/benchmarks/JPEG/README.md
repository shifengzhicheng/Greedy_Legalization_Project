# JPEG benchmark

本目录包含 JPEG / NG45 的 Bookshelf benchmark。

应包含：

- `JPEG.aux`
- `JPEG.nodes`
- `JPEG.nets`
- `JPEG.pl`
- `JPEG.scl`

运行示例：

```bash
bash run.sh JPEG
python main.py --case JPEG --mode baseline --out-dir results
```
