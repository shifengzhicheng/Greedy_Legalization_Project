# GCD benchmark

本目录包含 GCD / NG45 的 Bookshelf benchmark。

应包含：

- `GCD.aux`
- `GCD.nodes`
- `GCD.nets`
- `GCD.pl`
- `GCD.scl`

运行示例：

```bash
bash run.sh GCD
python main.py --case GCD --mode baseline --out-dir results
```
